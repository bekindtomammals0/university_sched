# Generated by AI-assisted tooling — reviewed by <user>

[tool.poetry]
name = "university-sched"
version = "0.1.0"
description = "Exam Scheduling Application"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"
packages = [{include = "university_sched"}]

[tool.poetry.dependencies]
python = "^3.10"
sqlalchemy = "^2.0.28"
pydantic = "^2.6.4"
alembic = "^1.13.1"
psycopg2-binary = "^2.9.9" # For PostgreSQL interaction
pandas = "^2.2.1"
pyyaml = "^6.0.1"

[tool.poetry.group.dev.dependencies]
pytest = "^8.1.1"
black = "^24.3.0"
isort = "^5.13.2"
flake8 = "^7.0.0"
mypy = "^1.9.0"
hypothesis = "^6.99.13"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.black]
line-length = 100
target-version = ['py310']

[tool.isort]
profile = "black"
line_length = 100
